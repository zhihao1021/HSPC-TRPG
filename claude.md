# Dungeons & Dragons 5e Discord Bot

你需要製作一個 TRPG Discord Bot，劇本使用 DnD 系統，這個 Bot 必須要做為 GM 的角色來管理與推進劇情，並且要能夠靈活應對玩家的各種行動與決策，而不是只是按照預設的劇情走向來進行。

這個機器人背後的主要目的是公布一串固定的晉級名單，但我希望透過加入 TRPG 的元素來讓這個過程更有趣和互動性。讓 Discord 中的玩家們能夠參與到這個過程中，透過不斷的冒險和挑戰來一步一步解鎖晉級名單，這樣不僅能增加玩家的參與感，還能讓整個過程更加生動有趣。

因此，你不能只是簡單地公布晉級名單，而是需要基於 DnD 的規則和劇情來設計一個完整的冒險過程，讓玩家們能夠在這個過程中做出各種決策，並且根據他們的行動來推進劇情，最終達到公布晉級名單的目的。整個遊戲的過程會持續 30 天左右，計畫每天公布一組晉級名單上的隊伍，因此你不能過快的推進劇情，而是需要根據玩家的行動來調整劇情的節奏。

接下來，請你開始設計這個 TRPG Discord Bot:
- 使用 openai 的套件串接 Deepseek v4 模型來生成劇情和回應玩家的行動，同時要能夠支援 thinking 模式。
- 使用 asyncpg 連接 PostgreSQL 數據庫來存儲玩家的行動、劇情進展和晉級名單等信息。
- 你需要設計合理的 Prompt 來引導模型生成符合 DnD 風格的劇情和回應，並且要能夠根據玩家的行動來調整劇情的走向；同時，你需要要求模型不能直接公布晉級名單，也不能過快的推進劇情，而是要根據玩家的行動來調整劇情的節奏，讓公布的間隔保持在每天一組隊伍左右。
- 你需要設計一些工具來幫助模型生成更符合 DnD 風格的劇情和回應。
    - 骰子工具：可以傳入骰子的類型和數量，返回骰子的結果。
    - 角色工具：可以透過 ID 查詢玩家角色的屬性、技能和裝備等信息，並且可以根據玩家的行動來更新這些信息。
- 當上下文長度超過模型的限制時，你需要設計一個機制來選擇性地保留重要的上下文信息，並且丟棄一些不太重要的信息，以確保模型能夠生成合理的回應。
- 你需要使用 .env 文來來儲存設定，例如 OpenAI API 金鑰、數據庫連接信息、上下文限制、模型選擇等，並且在代碼中使用這些設定來配置模型和數據庫連接等。當程式運行時可以透過 --env 來指定使用哪個 .env 文件，以便於在不同的環境中使用不同的設定。預設情況下，程式會使用 .env 文件中的設定。
- 你需要防範模型生成不適當的內容，例如暴力、色情、歧視等，並且要能夠根據玩家的反饋來調整模型的行為，以確保整個遊戲過程是安全和有趣的。
- 你還需要防範玩家對模型進行 Prompt Injection 攻擊，並且要能夠根據玩家的行動來調整模型的行為，以確保整個遊戲過程是安全和有趣的。
- 玩家透過直接在 Discord 頻道中發送消息來與模型互動，這些消息會被作為模型的輸入來生成劇情和回應，因此你需要確保這些消息能夠被正確地處理和理解，以便模型能夠生成合理的回應。
- 請注意，只有當資料庫中存在該頻道的 session 時，模型才會對玩家的消息做出回應；如果資料庫中不存在該頻道的 session，模型應該忽略玩家的消息，並且不做出任何回應，以確保模型能夠專注於正在進行的冒險過程。
- 每個頻道的 session 都會有一個獨立的劇情進展和玩家互動，因此你需要確保模型能夠根據不同頻道的 session 來生成不同的劇情和回應，以確保每個頻道的玩家都能夠有一個獨特的冒險體驗。
- 在給模型的訊息中除了玩家的原文以外，還需要包含以下額外資訊:
    - 當下的時間
    - 玩家的名稱
    - 玩家的 ID
- 如果在訊息中包含了 mention，你需要將 mention 正確的轉換為玩家的名稱與 ID，以確保模型能夠正確地理解玩家的行動和決策。
- 每個不同的使用者都會視為不同的玩家，並且每個玩家都會有自己的角色和屬性等信息，這些信息需要被存儲在數據庫中，並且在模型生成劇情和回應時能夠被正確地使用。如果模型使用角色工具來查詢玩家角色的信息，這些信息需要被正確地返回給模型，以確保模型能夠生成合理的回應；如果玩家還沒有角色信息，模型需要能夠根據玩家的行動來創建一個新的角色，並且將這些信息存儲在數據庫中，以便後續使用。
- 每則回覆訊息中除了模型生成的內容以外，還需要包含以下額外資訊:
    - 如果有進行擲骰，在回覆中需要有一個 Embed 包含了骰子的類型、數量和結果等信息，以確保玩家能夠清楚地看到擲骰的結果。這個訊息應該在工具被調用的時候自動生成，而不是由模型生成，以確保訊息的準確性和一致性。
- 除此之外，你還需要創建一些 slash command:
    - /start: 用來開始一個新的冒險，這個命令只能由該群組中擁有管理員權限的人員使用，使用後，你需要自行初始化這個 session 並將其存儲在數據庫中，以便後續的劇情推進和玩家互動都能夠基於這個 session 來進行。
    - /status: 用來查看當前冒險的狀態，這個命令可以由任何玩家使用，並且需要返回一些玩家的角色信息，以便玩家能夠了解當前的遊戲狀態。
- 晉級隊伍會透過 JSON 檔案提供，這個檔案會包含了每組隊伍的名稱以及建議公布的時間，你需要根據這個檔案來設計劇情的推進和公布晉級名單的節奏，以確保當時間接近時，模型能夠根據玩家的行動來調整劇情的走向，並且在適當的時候公布晉級名單，以確保整個過程是有趣和互動的。
- Prompts 請獨立於一個資料夾中存放，以便於維護
- 你需要確保整個代碼的結構清晰，並且有適當的模塊化，以便於後續的維護和擴展。

以上這些要求為基礎，請你開始設計和實現這個 TRPG Discord Bot，並且確保它能夠提供一個有趣和互動的冒險體驗給玩家們，同時也能夠達到公布晉級名單的目的。

請你先查看一下目前的代碼結構，並且根據上述要求來進行相應的修改和擴展，以確保整個系統能夠符合這些要求，並且能夠提供一個有趣和互動的冒險體驗給玩家們。

需要的環境與套件已經安裝好在 .venv 中，請你在這個環境中進行開發和測試，以確保整個系統能夠正常運行並且符合上述要求。
